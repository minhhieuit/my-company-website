@model IEnumerable<MVC_Thehegeo.Models.BlogModels.SliderModel>
<div class="section">
    <div id="about-slider" class="owl-carousel owl-theme">
        @foreach (var item in Model)
        {
            <img class="img-responsive" src="@item.ImageLink" alt="">
        }
        
    </div>
</div>
